# Set ONE of these to use a real LLM. If none is set, the app runs in
# MOCK_LLM mode (deterministic canned responses) so it still runs and
# tests still pass with zero API keys / zero cost.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LLM_PROVIDER=mock   # mock | openai | anthropic
